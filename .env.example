# Copying this file is optional. Never put a real cookie or token in a file.
# scripts/remote.ps1 prompts for the cookie with hidden input and keeps it only
# in the child process environment.
AI_COMIC_JUPYTER_TOKEN=amd-oneclick

